# Copy to a root-readable file outside the repository (mode 0600).
# Never commit or print the real control-plane key.
CONTROL_PLANE_API_KEY=
ECHO_PACT_MCP_AGENT_ID=agt_REPLACE_ME
ECHO_PACT_MCP_SNAPSHOT_POINTER=/var/lib/echo-pact/active.json
PYTHONDONTWRITEBYTECODE=1
PYTHONUNBUFFERED=1
